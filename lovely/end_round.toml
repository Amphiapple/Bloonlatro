[manifest]
version = "1.0.0"
dump_lua = true
priority = 1

# Mdom active check
[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = "G.GAME.blind.in_blind = false"
position = 'before'
payload = '''
local replay_boss = false
local dominus = find_joker('Glaive Dominus')
if #dominus > 0 then
    replay_boss = true
    for k, v in pairs(dominus) do
        if v.ability.extra.active == false then
            replay_boss = false
        end
    end 
end

local last_segment = true
if Bloonlatro.boss_challenge and Bloonlatro.boss_challenge[3] and G.GAME.round_resets.ante%G.GAME.win_ante == 0 then
    local current_blind = G.GAME.blind and G.GAME.blind.config and G.GAME.blind.config.blind or nil
    last_segment = Bloonlatro.get_boss_challenge_blind(3).key == (current_blind and current_blind.key or nil)
end
'''
match_indent = true

# Mdom win check
[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = "if G.GAME.round_resets.ante == G.GAME.win_ante and G.GAME.blind:get_type() == 'Boss' then"
position = 'at'
payload = "if G.GAME.round_resets.ante == G.GAME.win_ante and G.GAME.blind:get_type() == 'Boss' and last_segment and not replay_boss then"
match_indent = true

# Mdom ante end check
[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = "if G.GAME.blind:get_type() == 'Boss' then"
position = 'at'
payload = "if G.GAME.blind:get_type() == 'Boss'and last_segment and not replay_boss then"
match_indent = true

# Mdom boss defeat check
[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''
G.GAME.current_round.voucher = SMODS.get_next_vouchers()
G.GAME.round_resets.blind_states.Boss = 'Defeated'
for k, v in ipairs(G.playing_cards) do
    v.ability.played_this_ante = nil
end
'''
position = 'at'
payload = '''
if replay_boss then
    for k, v in pairs(dominus) do
        v.ability.extra.active = false
    end
else
    G.GAME.current_round.voucher = SMODS.get_next_vouchers()
    G.GAME.round_resets.blind_states.Boss = 'Defeated'
    for k, v in ipairs(G.playing_cards) do
        v.ability.played_this_ante = nil
    end
end
'''
match_indent = true

# Reset round variables
[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = "reset_idol_card()"
position = 'after'
payload = '''
reset_desperado_card()
reset_spike_factory_card()
'''
match_indent = true
